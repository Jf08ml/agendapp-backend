services:
  backend:
    build: .
    ports:
      - "127.0.0.1:5000:5000"
    env_file:
      - .env.production
    restart: unless-stopped
    logging:
      options:
        max-size: "10m"
        max-file: "3"
