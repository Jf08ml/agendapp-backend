# 🔒 Instalación de Paquetes de Seguridad

## Instalar dependencias requeridas

```bash
cd agenda-backend
npm install express-rate-limit helmet
```

## Paquetes instalados

1. **express-rate-limit** - Protección contra DDoS y brute force
2. **helmet** - Headers de seguridad HTTP

## Después de instalar

Los cambios en `src/app.js` ya están listos para usar estos paquetes.

Simplemente ejecuta:
```bash
npm install express-rate-limit helmet
```

Y reinicia el servidor.
